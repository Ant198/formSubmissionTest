version: "3"
services:
  selenium:
    image: selenium/standalone-chrome:134.0
    container_name: selenium
    ports:
      - "4444:4444"

  tests:
    build: .
    container_name: test_form
    depends_on:
      - selenium
    environment:
      SELENIDE-REMOTE: "http://selenium:4444/wd/hub"
